Submitted work lives here, one folder per commission, each with an `index.html`
at its root. These are committed deliberately — the repository is the storage,
and this folder is what gets served from the apps origin.
